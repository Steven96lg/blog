---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
const posts = (await getCollection('posts')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---
<div class="content-blog-posts">
    <div class="content-blog-header">
        <h2>¡Bienvenido a mi blog, te saluda Steven Layna!</h2>
        <span class="header-description">En este blog comparto conocimiento sobre desarrollo web, algoritmos, buenas prácticas de programación y mi recorrido en el mundo tech. Cada artículo tiene como objetivo ayudarte a mejorar tus habilidades, aprender junto conmigo y mostrar mi trayectoria y descubrimientos día a día.</span>
        <p>¡Espero que lo disfrutes al máximo 🚀 y te sea de gran utilidad 💡!"</p>
    </div>
    <div class="blog-posts">
        {posts.map((post) => (
            <article>
                <div class="content-img">
                    <Image 
                        src={post.data.image} 
                        alt={post.data.title}
                        width={300}
                        height={250}
                        class="post-thumbnail"
                    />
                </div>
                <a href={`/blog/post/${post.slug}`}>{post.data.title}</a>
                <div class="tags-date">
                    <span class="tag-post">{post.data.tag}</span>
                    <div class="date-post">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"><g fill="none"><rect width="18" height="15" x="3" y="6" stroke="currentColor" stroke-width="2" rx="2"/><path fill="currentColor" d="M3 10c0-1.886 0-2.828.586-3.414S5.114 6 7 6h10c1.886 0 2.828 0 3.414.586S21 8.114 21 10z"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M7 3v3m10-3v3"/></g></svg>
                        <span>{new Date(post.data.pubDate).toLocaleDateString('es-MX', { year: 'numeric', month: 'short', day: 'numeric' })}</span>
                    </div>
                </div>
                <p>{post.data.description}</p>
            </article>
        ))}
    </div>
</div>

<style>

    .content-blog-posts .content-blog-header{
        padding: 20px 0 20px 20px;
        border-radius: 10px;
        background-color: ;
    }

    .content-blog-posts h2{
       color: white;
       margin: 0;
       font-size: 30px;
    }

    .content-blog-posts .header-description{
        color: white;
        margin-top: 10px;
        font-family: Arial, Helvetica, sans-serif;
        font-size: 14px;
        display: inline-block;
        line-height: 1.3rem;
    }

    .content-blog-posts p{
        color: white;
        font-style: italic;
    }

    .blog-posts{
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        margin-top: 16px;
        place-items: center;
    }

    .blog-posts .tags-date{
        display: flex;
        justify-content: space-around;
        padding: 0 12px;
        font-family: "Archivo Narrow", sans-serif;
        font-size: 1rem;
        color: #888;
        margin-top: 0.9rem;
        & .tag-post{
            background-color: black;
            color: white;
            padding: 5px 10px;
            display: inline-block;
            margin-right: 20px;
        }
        & .date-post{
            display: flex;
            align-items: center;
            background-color: rgb(44, 146, 172);
            padding: 0 10px;
            color: white;
            & span{
                padding-left: 5px;
            }
        }
    }

    .blog-posts article{
        width: 300px;
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-bottom: 40px;
    }

    .blog-posts article .content-img{
        width: 300px;
        height: 250px;
      
    }

    .blog-posts article img{
        width: 300px;
        height: 250px;
        object-fit: contain;
        border-radius: 8px;
    }

    .blog-posts article a{
        text-decoration: none;
        color: white;
        font-weight: bold;
        font-size: 1.2rem;
        display: block;
        margin-top: 0.7rem;
        padding: 0 12px;
        transition: all 0.3s ease;
    }

    .blog-posts article a:hover{
        color: black;
        background-color: white;
    }

    .blog-posts article p{
        padding: 0 12px;
        text-align: center;
        color: white;
        font-family: "Open Sans", sans-serif;
        font-size: 0.9rem;
        line-height: 1.5rem;
    }

     /* Responsive: Móviles */
    @media (max-width: 768px) {
        .blog-posts {
            grid-template-columns: 1fr;
        }

        .blog-posts article{
            width: 100%;
            height: auto;
        }

        .blog-posts article .content-img{
            width: 100%;
            height: 200px;
        }

        .blog-posts article img{
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 0;
        }

        .content-blog-posts h2{
            font-size: 24px;
        }

        .content-blog-posts .header-description{
            font-size: 13px;
        }
    }

    /* Responsive: Móviles pequeños */
    @media (max-width: 480px) {
        .blog-posts article{
            width: 100%;
            margin-bottom: 30px;
        }

        .blog-posts article .content-img{
            width: 100%;
            height: auto;
        }

        .blog-posts article img{
            width: 100%;
            height: auto;
        }

        .blog-posts article a{
            font-size: 1rem;
        }

        .content-blog-posts h2{
            font-size: 20px;
        }

        .content-blog-posts .header-description{
            font-size: 12px;
        }

        .blog-posts .tags-date{
            flex-direction: column;
            gap: 8px;
        }

        .blog-posts .tags-date .tag-post{
            margin-right: 0;
        }
    }
</style>